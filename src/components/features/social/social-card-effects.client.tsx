"use client";

/**
 * Social Brand Hover Effects Component
 *
 * This component imports the CSS styles for social media cards hover effects.
 * Moved from inline styles to external CSS file for better maintainability.
 */

import "../../../styles/social-styles.css";

export function SocialCardEffects() {
  // Component is now just an import wrapper for the CSS
  // CSS is imported at the top, no need for dangerouslySetInnerHTML
  return null;
}
