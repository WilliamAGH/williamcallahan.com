# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.next
node_modules
.jest-pre-compiled
dist
dist-ssr
*.local
.puppy*
coverage
.env
# Editor directories and files
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.early.coverage/**
 
tsconfig.tsbuildinfo

# Coverage directory
/coverage
/coverage/**
.env
.qodo

# Sentry Config File
.env.sentry-build-plugin

# Fetched/cached data directories (local dev caches)
/data/github-projects/**
/data/github-activity/**
/data/bookmarks/**

# Fetched logos in public directory (ignore all files, keep directory with .gitkeep if needed)
/data/images/logos/**
/data/images/bookmarks/**
.populate-volumes-last-run-success

.early.coverage

# Data update details file
.data-update-details.json

# Lint progress tracking
.lint-progress.json

.jrdev/
!.jrdev/.gitkeep
.aider*
.crush/
redocly-demo/
redoc-demo.html
# Claude Code user-local settings (per-user, not committed)
.claude/

# S3 reset audits directory
.s3-reset-audits/
bookmarks.json
github-activity.json

# Generated files (CSP hashes, bookmarks cache, etc.)
# These are created by build scripts and should not be committed
generated/*
!generated/.gitkeep
!generated/bookmarks/
generated/bookmarks/*
!generated/bookmarks/.gitkeep

# Legacy paths (can be removed once migration is complete)
lib/data/bookmarks-index.json
lib/data/slug-mapping.json
lib/data/bookmarks.json
src/lib/data/slug-mapping.json
src/lib/data/bookmarks.json
config/csp-hashes.json

# clerk configuration (can include secrets)
/.clerk/
