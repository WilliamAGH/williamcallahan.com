/**
 * Mock for image file imports in tests
 *
 * Returns a simple string that tests can use to verify images are being loaded.
 * Used for: .png, .jpg, .jpeg, .gif, .webp, .avif, .ico, .bmp, .svg
 */
export default "test-file-stub";
