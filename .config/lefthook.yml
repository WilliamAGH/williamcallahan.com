# lefthook.yml — Git hook configuration (replaces .husky/)
# Pre-commit: lint + format staged files (parallel)
# Pre-push: production build + verification gate (sequential)

pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,jsx,ts,tsx}"
      run: bunx oxlint -c config/oxlintrc.json --fix {staged_files}
      stage_fixed: true
    oxfmt:
      glob: "*.{js,jsx,ts,tsx}"
      run: bunx oxfmt -c config/oxfmtrc.json --write {staged_files}
      stage_fixed: true

pre-push:
  piped: true
  commands:
    build:
      run: USE_NEXTJS_CACHE=false SKIP_BUILD_VALIDATIONS=1 bun run build
    verify:
      run: bun run verify
